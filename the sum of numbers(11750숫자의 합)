#include <stdio.h>

int main()
{
    char a[101];
    int n,i,sum=0;
    
    scanf("%d", &n);
    
    scanf(" %s", a);
    
    for(i=0; i<n; i++)
    {
        sum+=a[i];
    }
    
    printf("%d", sum-n*48);
    
    return 0;
}
