#include <stdio.h>

int main()
{
    int i,tmp,sum=0;
    
    for(i=0; i<5; i++)
    {
        scanf(" %d", &tmp);
        
        if(tmp<40)  tmp=40;
        
        sum+=tmp;
    }
    
    printf("%d", sum/5);

    return 0;
}
