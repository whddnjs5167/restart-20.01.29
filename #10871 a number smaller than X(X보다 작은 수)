#include <stdio.h>

int main()
{
    int n,x, tmp, i;
    
    scanf("%d %d", &n, &x);
    
    for(i=0; i<n; i++)
    {
        scanf("%d", &tmp);
        
        if(x>tmp)   printf("%d ", tmp);
    }

    return 0;
}
